<section class="store-features">
  <h2 class="section-title">مميزات المتجر</h2>
  <div class="features-container">
    <!-- ميزة 1 -->
    <div class="feature-box fade-item">
      <div class="icon">
        <!-- أيقونة عملاء -->
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="#F5F5DC" width="42" height="42">
          <path d="M12 12c2.7 0 4.8-2.2 4.8-4.8S14.7 2.4 12 2.4 7.2 4.6 7.2 7.2 9.3 12 12 12zm0 2.4c-3.2 0-9.6 1.6-9.6 4.8v2.4h19.2v-2.4c0-3.2-6.4-4.8-9.6-4.8z"/>
        </svg>
      </div>
      <h3 class="counter" data-target="2793">0</h3>
      <p>+ عميل يحب منتجاتنا</p>
    </div>

    <!-- ميزة 2 -->
    <div class="feature-box fade-item">
      <div class="icon">
        <!-- أيقونة التقييم -->
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="#FFD700" width="42" height="42">
          <path d="M12 .587l3.668 7.431 8.2 1.192-5.934 5.78 1.4 8.174L12 18.897l-7.334 3.852 1.4-8.174L.132 9.21l8.2-1.192z"/>
        </svg>
      </div>
      <h3 class="counter" data-target="5">0</h3>
      <p>5/5 أفضل متجر</p>
    </div>

    <!-- ميزة 3 -->
    <div class="feature-box fade-item">
      <div class="icon">
        <!-- أيقونة عدد التقييمات الجديدة -->
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="#F5F5DC" width="42" height="42">
          <path d="M21 6h-2V4a2 2 0 0 0-2-2H7a2 2 0 0 0-2 2v2H3c-1.1 0-2 .9-2 2v11c0 1.1.9 2 
                   2 2h18c1.1 0 2-.9 2-2V8c0-1.1-.9-2-2-2zM7 4h10v2H7V4zm0 7h5v5H7v-5zm12 
                   9H3V8h2v2h14V8h2v12z"/>
        </svg>
      </div>
      <h3 class="counter" data-target="399">0</h3>
      <p>+ عدد التقييمات</p>
    </div>
  </div>
</section>

<style>
.store-features {
  background: #F5F5DC;
  color: #3A4F20;
  padding: 40px 15px;
  border-radius: 14px;
  text-align: center;
  direction: rtl;
  margin: 30px 0;
}

.section-title {
  font-size: 1.8rem;
  color: #556B2F;
  margin-bottom: 25px;
  font-weight: bold;
}

.features-container {
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 25px;
}

.feature-box {
  background: #6B8E23;
  padding: 25px 20px;
  border-radius: 14px;
  width: 190px;
  color: #fff;
  opacity: 0;
  transform: translateY(30px) scale(0.9);
  transition: opacity 0.8s ease, transform 0.8s ease;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
}

.feature-box.visible {
  opacity: 1;
  transform: translateY(0) scale(1);
}

.feature-box:hover {
  transform: translateY(-5px) scale(1.05);
  background: #556B2F;
}

.icon {
  margin-bottom: 12px;
}

.feature-box h3 {
  font-size: 1.9rem;
  color: #F5F5DC;
  margin: 6px 0;
  font-weight: bold;
}

.feature-box p {
  font-size: 0.95rem;
  color: #F5F5DC;
}

/* متجاوب */
@media (max-width: 768px) {
  .features-container {
    gap: 18px;
  }
  .feature-box {
    width: 150px;
    padding: 18px;
  }
  .feature-box h3 {
    font-size: 1.5rem;
  }
}
</style>

<script>
function animateCounterEaseOut(counter) {
  const target = +counter.getAttribute('data-target');
  let startTime = null;
  const duration = 2000;

  function easeOut(t) {
    return 1 - Math.pow(1 - t, 3);
  }

  function update(timestamp) {
    if (!startTime) startTime = timestamp;
    const progress = Math.min((timestamp - startTime) / duration, 1);
    const value = Math.floor(easeOut(progress) * target);
    counter.textContent = value;

    if (progress < 1) {
      requestAnimationFrame(update);
    } else {
      counter.textContent = target;
    }
  }

  requestAnimationFrame(update);
}

const featureBoxes = document.querySelectorAll('.feature-box');

const observer = new IntersectionObserver(entries => {
  entries.forEach((entry, index) => {
    const box = entry.target;
    const counter = box.querySelector('.counter');

    if (entry.isIntersecting) {
      setTimeout(() => box.classList.add('visible'), index * 200); // Stagger Animation
      if (counter) animateCounterEaseOut(counter);
    } else {
      box.classList.remove('visible');
      if (counter) counter.textContent = "0";
    }
  });
}, { threshold: 0.5 });

featureBoxes.forEach(box => observer.observe(box));
</script>






<style>
  #whatsapp-widget {
    position: fixed;
    bottom: 25px;
    right: 25px;
    z-index: 9999;
    transition: all 0.3s ease;
  }

  #whatsapp-widget:hover {
    transform: scale(1.1);
  }

  #whatsapp-popup {
    display: none;
    position: fixed;
    bottom: 90px;
    right: 25px;
    background-color: #fff;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
    padding: 20px;
    width: 300px;
    max-width: 90%;
    font-family: 'Segoe UI', 'Helvetica Neue', sans-serif;
    animation: fadeIn 0.3s ease;
    border: 1px solid #e0e0e0;
  }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  #whatsapp-popup:target {
    display: block;
  }

  #whatsapp-popup-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-weight: 600;
    color: #075e54;
    font-size: 16px;
    margin-bottom: 15px;
    padding-bottom: 10px;
    border-bottom: 1px solid #f0f0f0;
  }

  #whatsapp-popup-content {
    color: #555;
    font-size: 14px;
    line-height: 1.5;
  }

  #whatsapp-popup-content p {
    margin-bottom: 15px;
  }

  #whatsapp-popup a.button {
    display: block;
    background-color: #25d366;
    color: white;
    text-align: center;
    text-decoration: none;
    padding: 12px 20px;
    margin-top: 15px;
    border-radius: 6px;
    font-size: 15px;
    font-weight: 500;
    transition: background-color 0.3s ease;
    box-shadow: 0 2px 5px rgba(37, 211, 102, 0.3);
  }

  #whatsapp-popup a.button:hover {
    background-color: #128c7e;
  }

  #whatsapp-widget img {
    width: 56px;
    height: 56px;
    cursor: pointer;
    transition: transform 0.3s ease;
    filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.2));
  }

  .close-btn {
    text-decoration: none;
    color: #888;
    font-size: 22px;
    line-height: 1;
    transition: color 0.2s ease;
  }

  .close-btn:hover {
    color: #333;
  }

  /* تأثيرات للهواتف */
  @media (max-width: 768px) {
    #whatsapp-widget {
      bottom: 15px;
      right: 15px;
    }
    
    #whatsapp-popup {
      right: 15px;
      width: 280px;
    }
    
    #whatsapp-widget img {
      width: 50px;
      height: 50px;
    }
  }
</style>

<!-- أيقونة واتساب -->
<div id="whatsapp-widget">
  <a href="#whatsapp-popup"><img src="https://img.icons8.com/color/48/000000/whatsapp--v1.png" alt="WhatsApp" /></a>
</div>

<!-- البوب أب -->
<div id="whatsapp-popup">
  <div id="whatsapp-popup-header">
    <span>💬 خدمة العملاء</span>
    <a href="#" class="close-btn" aria-label="إغلاق">×</a>
  </div>
  <div id="whatsapp-popup-content">
    <p>مرحباً بك عزيزنا العميل،<br>نحن هنا لمساعدتك على مدار الساعة. لا تتردد في التواصل معنا عبر واتساب.</p>
    <a href="https://wa.me/01070682455" target="_blank" rel="noopener" class="button">
      💬 ابدأ المحادثة الآن
    </a>
  </div>
</div>