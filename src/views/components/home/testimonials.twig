{#
| Variable           | Type                      | Description                         |
|--------------------|---------------------------|-------------------------------------|
| limit              | string                    | Maximum number of the testimonials  |
| type               | string                    | `default:store` all, product, store |
| sort               | string                    |                                     |
| hide_customer_info | bool                      | show/hide info customers            |
#}
<section class="s-block s-block--testimonials container overflow-hidden">
  <h2 class="testimonials-title">آراء العملاء</h2>
  <salla-reviews 
    {{ display_all_url ? "display-all-link" : "" }} 
    limit="{{ limit }}" 
    sort="{{ sort }}" 
    {{ hide_customer_info ? 'hide-customer-info' : '' }} 
    type="{{type}}">
  </salla-reviews>
</section>

<style>
/* 🌟 ستايل عام للقسم */
.s-block--testimonials {
  background: linear-gradient(135deg, #f9f9f9, #ffffff);
  padding: 50px 20px;
  border-radius: 20px;
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.05);
  position: relative;
  overflow: hidden;
}

/* عنوان جذاب */
.testimonials-title {
  font-size: 28px;
  font-weight: bold;
  text-align: center;
  color: #333;
  margin-bottom: 30px;
  position: relative;
}
.testimonials-title::after {
  content: "";
  display: block;
  width: 60px;
  height: 4px;
  background: #ff6b6b;
  margin: 10px auto 0;
  border-radius: 2px;
}

/* كروت الريفيو */
salla-reviews {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 20px;
  animation: fadeIn 1s ease-in-out;
}

/* أنيميشن لكل كرت */
salla-reviews > * {
  background: #fff;
  border-radius: 15px;
  padding: 20px;
  box-shadow: 0 4px 15px rgba(0,0,0,0.05);
  opacity: 0;
  transform: translateY(30px);
  animation: cardFadeUp 0.8s ease forwards;
}
salla-reviews > *:nth-child(2) { animation-delay: 0.2s; }
salla-reviews > *:nth-child(3) { animation-delay: 0.4s; }
salla-reviews > *:nth-child(4) { animation-delay: 0.6s; }

/* 🌈 أنيميشن */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes cardFadeUp {
  0% { opacity: 0; transform: translateY(30px); }
  100% { opacity: 1; transform: translateY(0); }
}

/* 🟢 ريسبونسيف */
@media (max-width: 768px) {
  .s-block--testimonials {
    padding: 30px 15px;
  }
  .testimonials-title {
    font-size: 24px;
  }
}
</style>

